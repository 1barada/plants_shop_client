export default interface ISearchInfo {
    title?: string,
    minPrice?: number,
    maxPrice?: number,
    minWeight?: number,
    maxWeight?: number,
    minHeight?: number,
    maxHeight?: number,
}